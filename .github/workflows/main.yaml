# This workflow will build a package using Maven and then publish it to GitHub packages when a release is created
# For more information see: https://github.com/actions/setup-java/blob/main/docs/advanced-usage.md#apache-maven-with-a-settings-path

name: hello job

on:
  push:
    branches:
      - master
jobs:
  build:
   runs-on: self-hosted
   steps:
    - uses: actions/checkout@v1
    - name: Run a maven package
      run: mvn clean package
    - name: tomcat deploy
      run: curl -v -u admin:admin -T /home/ec2-user/actions-runner/_work/spring3-mvc-maven-xml-hello-world/spring3-mvc-maven-xml-hello-world/target/spring3-mvc-maven-xml-hello-world-1.4.war 'http://ec2-52-66-137-229.ap-south-1.compute.amazonaws.com:8080/manager/text/deploy?path=/github_action_spring'


